const trailContainer=document.getElementById("cursorTrail");if(trailContainer){const dots=[];const DOT_COUNT=14;let mouseX=window.innerWidth/2;let mouseY=window.innerHeight/2;for(let i=0;i<DOT_COUNT;i++){const dot=document.createElement("div");dot.className="trail-dot";trailContainer.appendChild(dot);dots.push({el:dot,x:mouseX,y:mouseY});}window.addEventListener("pointermove",(e)=>{mouseX=e.clientX;mouseY=e.clientY;});function animateCursorTrail(){let x=mouseX;let y=mouseY;dots.forEach((dot,i)=>{dot.x+=(x-dot.x)*0.25;dot.y+=(y-dot.y)*0.25;dot.el.style.left=dot.x+"px";dot.el.style.top=dot.y+"px";const scale=1-i/DOT_COUNT;dot.el.style.transform=`translate(-50%, -50%) scale(${scale})`;dot.el.style.opacity=scale;x=dot.x;y=dot.y;});requestAnimationFrame(animateCursorTrail);}animateCursorTrail();}